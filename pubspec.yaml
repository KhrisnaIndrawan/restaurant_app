name: restaurant_app
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  alert_banner: ^1.0.0
  convert: ^3.1.1
  flutter:
    sdk: flutter
  google_fonts: ^5.0.0
  http: ^1.0.0
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets: 
    - assets/restaurant.json
